---

# jenkins variables
project_name: docker-orcaslicer-snapmaker
external_type: github_stable
release_type: stable
release_tag: latest
ls_branch: master
external_artifact_check: |
  assets=$(curl -u "${{ '{{' }} secrets.CR_USER {{ '}}' }}:${{ '{{' }} secrets.CR_PAT {{ '}}' }}" -sX GET "https://api.github.com/repos/Snapmaker/OrcaSlicer/releases/tags/${EXT_RELEASE}" | jq -r '.assets[].browser_download_url')
  if grep -q "Snapmaker_Orca_Linux_ubuntu_2404" <<< "${assets}"; then
    artifacts_found="true"
  else
    artifacts_found="false"
  fi
repo_vars:
  - EXT_USER = 'Snapmaker'
  - EXT_REPO = 'OrcaSlicer'
  - BUILD_VERSION_ARG = 'SNAPMAKER_ORCA_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-orcaslicer-snapmaker'
  - CONTAINER_NAME = 'orcaslicer-snapmaker'
  - DOCKERHUB_IMAGE = 'linuxserver/orcaslicer-snapmaker'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/orcaslicer-snapmaker'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/orcaslicer-snapmaker'
  - DIST_IMAGE = 'ubuntu'
  - MULTIARCH = 'false'
  - CI = 'true'
  - CI_WEB = 'true'
  - CI_PORT = '3001'
  - CI_SSL = 'true'
  - CI_DELAY = '120'
  - CI_WEB_SCREENSHOT_DELAY = '30'
  - CI_DOCKERENV = 'TZ=US/Pacific'
  - CI_AUTH = 'user:password'
  - CI_WEBPATH = ''
